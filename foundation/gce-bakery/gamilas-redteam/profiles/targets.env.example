# gamilas-redteam — copy to targets.env (gitignored) and edit for your run.
# Loaded automatically by profiles/lib/common.sh if present.

# The host you OWN / are authorized to test.
TARGET_HOST=yamato-dev.iq9.io
# TARGET_URL defaults to https://${TARGET_HOST}; override only for non-standard ports.
# TARGET_URL=https://yamato-dev.iq9.io

WIKI_PATH=/wiki
PUBLIC_PATH=/

# Profile 2 only — the stolen-credential material you are demonstrating with.
# STOLEN_IAP_COOKIE=GCP_IAAP_AUTH_TOKEN=...
# STOLEN_SA_KEY=/home/packer/leaked-sa.json
# DB_HOST=10.10.0.3
